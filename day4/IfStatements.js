// Day 4: Conditional Statements
//should implement user input like scanner in java
let word = "My favourite apples are yellow in colour.";
let isApple = false;
if (word.includes("apple")) {
    isApple = true;
    console.log(isApple);
} else {
    console.log("The word has no " + word)
}

